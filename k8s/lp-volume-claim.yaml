apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nodc-linkedpipes-claim
  labels:
    app.kubernetes.io/name: nodc
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ""
  resources:
    requests:
      storage: 256Gi
  volumeName: nodc-linkedpipes-pv
